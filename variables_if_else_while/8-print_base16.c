#include <stdio.h>

/**
 * main - Entry point
 * that only test
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num = '0';

	for (num = '0'; num <= '15'; num++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
