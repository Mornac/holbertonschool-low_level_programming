#include <stdio.h>

/**
 * main - Entry point
 * that only test
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 0;

	for (n = 0; n <= 15; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
