#include <stdio.h>

/**
 * main - Entry point
 * that only test
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (int i = 'a'; i <= 'z' ; i++)
	{
		putchar(i);
		putchar(' ');
	}
	return (0);
}
