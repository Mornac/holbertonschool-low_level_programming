#include <stdio.h>

/**
 * main - Entry point
 * that only test
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	for (int n = 'a'; n <= 'z' ; n++)
	{
		putchar(n);
		putchar(' ');
	}
	return (0);
}
