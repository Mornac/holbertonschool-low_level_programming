#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * that only test
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (int i = 'a'; i <= 'z' ; i++)
	{
		putchar(i);
		putchar(' ');
	}
	return (0);
}
